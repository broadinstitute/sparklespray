Move monitor into dashboard-backend
- Add endpoint for querying monitor status (state, log : list of {timestamp, severity, message})

Add UI pages for:
- list of all workpools
- On front page only show "active" workpools
- list of events (timestamp, type, workpool_id, task_id, job_id) (click to expand and see all fields on event)

On workpool summary tile:
- Add current workers and expected workers
- Add timeline (fixed to last 10 minutes. Likely will want to move to configurable window of time)

Make long log output gracefully handled. (Maybe make a common timeline control for what region of time we're viewing)
Various reported metrics all look wrong. specifically: CPU and memory. (Maybe drop proccess count)

Worker and batchapirequest status seem like they might be acting funny.

I think I saw us request 2 workers when we only needed one.

Keep an eye on latency between job submission and start

The link to the task output shouldn't be visible until after task completion

Add a link on task page to worker which owns the task. Add a link on worker to vm instance on compute page.

Various tables (workers, batchapirequests) should sort by create date


On the task details page make the following changes:
1. only show the link to the log if the task is in a terminal state
2. Add a link to the workpool page for the workpool associated with this task]


Define job submission file format

Fix metrics (test on hermit machine)
- CPU (Currently % of capacity, but would like % of 1 core)
- Memory (Verify this is working)
- Verify disk usage computed right?
- Maybe just make a plot of resident memory usage?
- Replace completed metrics with those computed from sampling
   - mean CPU time
   - mean resident size
   - max RSS
   - max disk usage?

lingering worker not working

Add output location to tasks

define job spec

workpool
  "machineType": "n2-standard-2",
  "rootDir": "/mnt/sparkles",
  (store in project)"region": "us-central1",
  "zones": compute
  "maxWorkerCount": 2,
  "maxPreemptibleWorkerAttempts": 0,
  "maxWorkersPerRequest": 2,
  "resources": [{ "name": "slots", "value": 1 }],
  "emptyVolumes": [],
  (store in project)"sparklesWorkerGCSPath": "gs://sparkles-test-0625/bin/sparkles-linux-amd64-dev",
  (store in project)"serviceAccount": "sparkles-lnibyae4xk@sparkles-test-0625.iam.gserviceaccount.com",
  "dbName": "sparkles",
  "lingerTimeSec": 600


------- 
Next steps:
1. Define submit command documentation. 
2. Define backend submission API
3. Implement submission API and submit command

Things that are missing: API key support. 

new sparkles config=
{"url": "...", "api_key": "..."}

sparkles sub job.json [--config path.json] [--name name (default to the filename)]

format of job.json:

{
  (default) "resources": [{ "name": "slots", "value": 1 }],
  (default) "filesToLocalize": [], 
  "tasks": [
    {
      (default to job+index) "name": ...
      "dockerImage": "alpine:3.19",
      "command": ["sh", "-c", "echo task 1 && sleep 5"],
      "filesToLocalize": ... 
    },
    ...
  ]
  or "task_template": {"dockerImage": ..., "command": ...} if --params filename.csv is specified
   "workpool": {
      "machineType": "n2-standard-2",
      (default) "rootDir": "/mnt/sparkles",
      (default from project) "region": "us-central1",
      (default by querying for region) "zones": ["us-central1-a", "us-central1-b", "us-central1-c", "us-central1-f"],
      (default) "resources": [{ "name": "slots", "value": 1 }],
      (default) "emptyVolumes": [], 
      (default from project) "sparklesWorkerGCSPath": "gs://sparkles-test-0625/bin/sparkles-linux-amd64-dev",
      (set by backend) "serviceAccount": "sparkles-lnibyae4xk@sparkles-test-0625.iam.gserviceaccount.com",
      (set by backend) "dbName": "sparkles",
      (default) "lingerTimeSec": 600

      // mutable fields
      (default) "maxWorkerCount": 1,
      (default) "maxPreemptibleWorkerAttempts": 0,
      (default) "maxWorkersPerRequest": 50,
   }
},


Minimal API submission:
{
  "tasks": [
    {
       "image": "alpine:3.19",
       "command": ["sh", "-c", "echo task 1 && sleep 5"],
   }
  ],
   "workpool": {
      "machineType": "n2-standard-2",
   }
}

Full reference

JobSubmission Fields 
name: tasks
type: list of TaskSpec
description:
default value:

TaskSpec Fields 
name: image
type: string
description: The docker image to use for executing the command
default value: None -- field is required

name: command: 
